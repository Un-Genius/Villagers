function scr_player_collision() {
	// MOVEMENT
	//moveSpd = moveS;

	// ANIMATION
	//anim_speed = 12;
	//anim_length = 9;

	//Collision
	/*
	if(sprite_index = spr_player_collision0) x -= 10;
	if(sprite_index = spr_player_collision1) y += 10;
	if(sprite_index = spr_player_collision2) x += 10;
	if(sprite_index = spr_player_collision3) y -= 10;
	*/
	/*
	my_dir = point_direction(obj_player.x, obj_player.y, obj_house0.x, obj_house0.y)
	
	if !place_empty(obj_player.x, obj_player.y, obj_house0) {
		x += lengthdir_x(move_speed, dir);
		y += lengthdir_y(move_speed, dir);
		
	}*/


}
